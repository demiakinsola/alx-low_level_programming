#include "main.h"

/**
* _strlen - This returns the length of a string.
* @s: Input pointer parameter.
* Return: length.
*/

int _strlen(char *s)
{
	int length;
	int i;

	for (i = 0; s[i]; i++)
	{
		length = i + 1;
	}

	return (length);
}
